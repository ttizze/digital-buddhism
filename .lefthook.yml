# lefthook設定ファイル
# コミット対象だけをoxfmtで修正し、oxlintで検査する
no_tty: true

pre-commit:
  commands:
    oxfmt:
      run: bunx oxfmt --write --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
    oxlint:
      run: bunx oxlint --no-error-on-unmatched-pattern {staged_files}
