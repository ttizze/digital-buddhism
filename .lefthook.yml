# lefthook設定ファイル
# コミット前とプッシュ前にbiomeを実行する
# （post-checkout等でwrite系コマンドを走らせるとブランチ切替のたびに作業ツリーが汚れるため登録しない）
no_tty: true

pre-commit:
  parallel: true
  commands:
    biome:
      run: bun run biome
      stage_fixed: true

pre-push:
  parallel: true
  commands:
    biome:
      run: bun run biome
