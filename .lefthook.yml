# lefthook設定ファイル
# コミット対象だけをBiomeで修正し、修正結果も同じcommitへ含める
no_tty: true

pre-commit:
  commands:
    biome:
      run: bunx @biomejs/biome check --write --files-ignore-unknown=true --no-errors-on-unmatched {staged_files}
      stage_fixed: true
